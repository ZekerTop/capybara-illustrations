interface:
  display_name: "Capybara Illustrations"
  short_description: "Turn creator content into relaxed capybara illustration concepts and images"
  default_prompt: "Use $capybara-illustrations to turn this idea or content into a relaxed capybara illustration, using the skill's default visual settings unless I specify otherwise."
policy:
  allow_implicit_invocation: true
